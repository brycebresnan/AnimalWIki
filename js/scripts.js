//  if animal = emperor penguin do penguin di 
else if frog = do the frog stuff
else if two toed sloth stuff
else 'we don't have information on that Animal

window.onload = function() {
  animal.style.display = "none";
}

const form = document.querySelector("form");
form.onsubmit = function(event) {
  
}

Form.apply.onsubmit = function(event) {

}